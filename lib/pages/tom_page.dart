import 'package:flutter/material.dart';

class TomPage extends StatelessWidget {
  const TomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

      body: Center(
        child: Text("This is the Tomorrow page"),
      ),
    );
  }
}